<nav class="navbar navbar-inverse">
  <div class="container">
    <div class="navbar-header">
      <%= link_to 'Activity Tracker', root_path, class: 'navbar-brand' %>
    </div>
    <div id="navbar">
      <ul class="nav navbar-nav">
      <% if user_signed_in? %>
        <li><%= link_to "#{current_user.name} Tasks", root_path, class: "navtext" %>
        </li>
        <% if params[:task_id] %>
        <li><%= link_to "#{Task.find(params[:task_id]).name} Activities", task_tactivities_path(params[:task_id]), class: "navtext" %>
        </li>
        <% end %>
        <% end %>
      </ul>
      <ul class="nav navbar-nav pull-right">
        <% if user_signed_in? %>
         <li><%= link_to 'Profile', edit_user_registration_path, class: "navtext" %></li>
        <li><%= link_to 'Log out', destroy_user_session_path, class: "navtext", method: :delete %></li>
        <% else %>
          <li><%= link_to 'Log In', new_user_session_path, class: "navtext" %></li>
          <li><%= link_to 'Sign Up', new_user_registration_path, class: "navtext" %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
